Vagrant.configure("2") do |config|
    config.vm.box = "vinaypatil/ubuntu-18.04-cert-manager-dev-box"
    config.vm.box_version = "0.0.1"

    config.vm.provider "virtualbox" do |v|
        v.memory = 2048*4
        v.cpus = 4
    end
    
end